import WeatherApi from '~/service/index';

const DayDetail = (forecastApi) => {
    console.log(WeatherApi);

    return (
        <div>
            <h2>Day Detail</h2>
        </div>
    );
};
export default DayDetail;
